package models

type (
	// control point static infomation
	Info struct {
		// name
		Name string
		// unique identifier of the control point
		Code string
		// version
		Version string
		// description
		Desc string
	}
)
